# Forward all DHCP server logs to backend syslog server
# DHCP logs typically use daemon facility

# Forward all logs to backend
*.* @backend:514

# Keep local logging as well
daemon.* /var/log/dhcpd.log
